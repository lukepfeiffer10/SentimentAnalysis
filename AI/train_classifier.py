# python script to train a classifier based on corpus and write to file pickle dump 'nb_classifier'

# note: moved to featx.py as a function. This is only a script.

from featx import train_classifier
train_classifier()